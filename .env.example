# Clé de chiffrement pour les données sensibles
# IMPORTANT: Générer une clé unique avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NE JAMAIS commiter le fichier .env avec la vraie clé!
ENCRYPTION_KEY=votre_cle_de_chiffrement_32_bytes_en_hex
